c0 = int(input("Please enter a number to test Collatz's hypothesis: "))
steps = 0

while c0 != 1:
    print(c0)
    steps += 1
    if c0 % 2 == 0:
        c0 = c0 // 2
    else:
        c0 = 3 * c0 -1
    if c0 == 1:
        print(c0)

print("It took", steps,"steps.")
