blocks_total = int(input("Enter the number of blocks: "))
height = 0
blocks_remaining = blocks_total
while blocks_remaining >= height + 1:
    height += 1
    blocks_remaining -= height
    

print("The height of the pyramid:", height)
